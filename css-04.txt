.heal{color:#7ee08a}
.dmg.crit{font-size:24px;color:#ffd166}
.dmg.shield{color:#c79ee0}
.dmg.rage{color:#ff5b3a;font-size:19px}
.dmg.adv{color:#ffd166;font-size:20px}
.dmg.exp{color:#7ee08a;font-size:12px}
.dmg.dis{color:#ff5b3a;font-size:22px;text-shadow:0 0 16px rgba(255,60,30,1),0 2px 4px #000}
.dmg.zone{color:#ff5b3a;font-size:20px;text-shadow:0 0 14px rgba(255,60,30,1),0 2px 4px #000}
@keyframes dmgUp{
  0%{opacity:0;transform:translate(-50%,-30%) scale(.5)}
  22%{opacity:1;transform:translate(-50%,-75%) scale(1.25)}
  100%{opacity:0;transform:translate(-50%,-160%) scale(1)}}
.slash{position:absolute;width:var(--tile);height:var(--tile);
  pointer-events:none;z-index:30;display:flex;align-items:center;justify-content:center}
.slash::before{content:"";position:absolute;inset:12%;border-radius:50%;
  border:2px solid rgba(255,120,90,.95);
  box-shadow:0 0 26px rgba(255,90,60,.9);
  animation:slashOut .55s ease-out forwards}
@keyframes slashOut{0%{transform:scale(.3);opacity:1}100%{transform:scale(1.8);opacity:0}}
.skill-fx{position:absolute;width:var(--tile);height:var(--tile);
  pointer-events:none;z-index:30}
.skill-fx::before{content:"";position:absolute;inset:10%;border-radius:50%;
  border:2px solid rgba(200,140,240,.95);
  box-shadow:0 0 30px rgba(155,89,182,.9);
  animation:slashOut .7s ease-out forwards}
.heal-fx{position:absolute;width:var(--tile);height:var(--tile);
  pointer-events:none;z-index:30}
.heal-fx::before{content:"";position:absolute;inset:18%;border-radius:50%;
  background:radial-gradient(circle,rgba(126,224,138,.6),transparent 70%);
  animation:healOut .8s ease-out forwards}
@keyframes healOut{0%{transform:scale(.4);opacity:0}40%{opacity:1}100%{transform:scale(1.5);opacity:0}}
.lv-fx{position:absolute;width:var(--tile);height:var(--tile);
  pointer-events:none;z-index:40}
.lv-fx::before{content:"LEVEL UP";position:absolute;top:50%;left:50%;
  transform:translate(-50%,-50%);
  font-family:var(--font-mono);font-size:10px;
  color:#ffd166;letter-spacing:.1em;white-space:nowrap;
  text-shadow:0 0 14px rgba(255,200,80,1);
  animation:lvUp 1.4s ease-out forwards}
@keyframes lvUp{
  0%{opacity:0;transform:translate(-50%,-20%) scale(.5)}
  25%{opacity:1;transform:translate(-50%,-50%) scale(1.1)}
  80%{opacity:1;transform:translate(-50%,-90%) scale(1)}
  100%{opacity:0;transform:translate(-50%,-120%) scale(.9)}}

.dis-fx{position:absolute;width:var(--tile);height:var(--tile);
  pointer-events:none;z-index:35;display:flex;align-items:center;justify-content:center}
.dis-fx.meteor::before{content:"☄";position:absolute;inset:0;
  display:flex;align-items:center;justify-content:center;
  font-size:calc(var(--tile) * .9);color:#ff7a3a;
  text-shadow:0 0 30px #ff4a10,0 0 60px #ff2a00;
  animation:meteorFall .85s ease-out forwards}
@keyframes meteorFall{
  0%{transform:translate(-60%,-120%) scale(.4);opacity:0}
  40%{opacity:1}
  100%{transform:translate(0,0) scale(1.4);opacity:0}}
.dis-fx.thunder::before{content:"⚡";position:absolute;inset:0;
  display:flex;align-items:center;justify-content:center;
  font-size:calc(var(--tile) * .95);color:#ffd166;
  text-shadow:0 0 26px #ffb300,0 0 50px #ff8800;
  animation:thunderStrike .6s ease-out forwards}
@keyframes thunderStrike{
  0%{transform:scale(.3) rotate(-20deg);opacity:0}
  30%{opacity:1;transform:scale(1.2) rotate(0)}
  100%{transform:scale(1.6);opacity:0}}
.dis-fx.quake::before{content:"";position:absolute;inset:0;
  border-radius:50%;border:2px solid rgba(200,160,80,.9);
  animation:quakeRing .9s ease-out forwards}
@keyframes quakeRing{
  0%{transform:scale(.3);opacity:1}
  100%{transform:scale(1.8);opacity:0}}

.start{position:fixed;inset:0;z-index:80;
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  padding:24px;background:radial-gradient(ellipse at 50% 40%,rgba(22,18,30,.95),rgba(4,4,8,.98));
  backdrop-filter:blur(8px);transition:opacity .5s,visibility .5s;overflow-y:auto}
.start.hide{opacity:0;visibility:hidden;pointer-events:none}
.start h2{font-family:var(--font-serif);font-weight:700;
  font-size:clamp(22px,5vw,32px);letter-spacing:.36em;text-indent:.36em;
  color:#f0e2b0;margin-bottom:10px;margin-top:20px;
  text-shadow:0 0 34px rgba(212,175,55,.45)}
.start p{font-size:11.5px;letter-spacing:.32em;text-indent:.32em;color:#7d7768;margin-bottom:24px;font-weight:300}
.fcards{display:flex;gap:16px;flex-wrap:wrap;justify-content:center;padding-bottom:20px}
.fcard{width:250px;padding:24px 18px 20px;
  border:1px solid color-mix(in srgb,var(--c) 45%,transparent);border-radius:10px;
  background:radial-gradient(ellipse at 50% 0%,color-mix(in srgb,var(--c) 15%,transparent),transparent 65%),
             linear-gradient(180deg,rgba(255,255,255,.03),rgba(255,255,255,.005));
  cursor:pointer;text-align:center;font-family:inherit;color:inherit;
  transition:transform .35s var(--ease-smooth),box-shadow .35s var(--ease-smooth);
  position:relative;overflow:hidden;transform-style:preserve-3d}
.fcard::after{content:"";position:absolute;top:0;left:0;right:0;height:2px;
  background:linear-gradient(90deg,transparent,var(--c),transparent)}
.fcard:hover{box-shadow:0 26px 52px -18px var(--c),0 0 0 1px var(--c) inset}
.fcard .fn,.fcard .fl,.fcard .fd,.fcard .fskill{transform:translateZ(24px);
  transition:transform .35s var(--ease-smooth)}
.fcard .fn{font-family:var(--font-serif);font-weight:700;
  font-size:28px;letter-spacing:.1em;color:#fff;
  text-shadow:0 0 26px var(--c)}
.fcard .fl{font-size:11px;letter-spacing:.32em;text-indent:.32em;color:var(--c);margin-top:9px;font-weight:500}
.fcard .fd{font-size:11.5px;color:#8d8578;line-height:1.85;margin-top:14px;
  letter-spacing:.05em;padding-top:12px;border-top:1px dashed rgba(255,255,255,.1);font-weight:300}
.fcard .fskill{margin-top:10px;padding:9px 11px;border-radius:6px;
  background:color-mix(in srgb,var(--c) 12%,transparent);
  border:1px solid color-mix(in srgb,var(--c) 30%,transparent);
  font-size:11px;line-height:1.65;color:#a89a7a;letter-spacing:.04em;font-weight:300}
.fcard .fskill b{display:block;color:var(--c);font-weight:500;
  margin-bottom:4px;letter-spacing:.12em;font-size:11.5px}

.over{position:fixed;inset:0;z-index:90;
  display:flex;align-items:center;justify-content:center;
  background:rgba(4,4,8,.93);backdrop-filter:blur(12px);
  opacity:0;visibility:hidden;transition:.5s;padding:20px}
.over.show{opacity:1;visibility:visible}
.over-box{width:100%;max-width:620px;max-height:92vh;overflow-y:auto;
  border:1px solid rgba(212,175,55,.35);border-radius:14px;
  background:linear-gradient(180deg,rgba(20,20,30,.98),rgba(8,8,12,.99));
  box-shadow:0 0 110px -24px rgba(212,175,55,.7),inset 0 1px 0 rgba(255,255,255,.04);
  padding:36px 32px 28px;position:relative;
  transform:translateY(26px) scale(.96);
  transition:transform .55s var(--ease-smooth)}
.over.show .over-box{transform:none}
.over-box::-webkit-scrollbar{width:6px}
.over-box::-webkit-scrollbar-thumb{background:rgba(212,175,55,.4);border-radius:3px}
.over-box::before{content:"";position:absolute;top:0;left:12%;right:12%;height:2px;
  background:linear-gradient(90deg,transparent,var(--ac,#d4af37),transparent);
  box-shadow:0 0 24px var(--ac,#d4af37);border-radius:2px}
.over-emblem{text-align:center;font-size:44px;line-height:1;
  margin-bottom:14px;filter:drop-shadow(0 0 26px var(--ac,#d4af37));
  animation:emblemIn .9s var(--ease-smooth) both}
@keyframes emblemIn{from{opacity:0;transform:scale(.4) rotate(-18deg)}to{opacity:1;transform:none}}
.over .ot{font-family:var(--font-serif);font-weight:900;
  font-size:clamp(26px,7vw,44px);letter-spacing:.24em;text-indent:.24em;
  text-align:center;line-height:1.2;
  background:linear-gradient(180deg,#fffbe8,var(--ac,#e8cf7a) 50%,#8a7326);
  -webkit-background-clip:text;background-clip:text;color:transparent;
  filter:drop-shadow(0 0 30px var(--ac,#d4af37))}
.over .os{margin-top:14px;font-size:12.5px;letter-spacing:.26em;text-indent:.26em;
  color:#a89a7a;text-align:center;line-height:1.9;font-weight:300}
.over-divider{height:1px;margin:24px 0 20px;
  background:linear-gradient(90deg,transparent,rgba(212,175,55,.35),transparent)}
.over-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:9px}
.os-item{padding:13px 8px;border-radius:9px;
  background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);
  text-align:center;transition:.3s}
.os-item:hover{background:rgba(255,255,255,.06);border-color:rgba(212,175,55,.3)}
.os-label{font-size:9.5px;letter-spacing:.22em;color:#7d7768;margin-bottom:6px;font-weight:400}
.os-val{font-size:21px;color:#f0d879;
  font-family:var(--font-mono);font-weight:700;
  text-shadow:0 0 18px rgba(212,175,55,.45);line-height:1.1}
.os-val small{font-size:11px;color:#7d7768;letter-spacing:.06em;
  text-shadow:none;margin-left:2px;font-weight:400}
.os-item.wide{grid-column:span 3;
  background:linear-gradient(135deg,rgba(255,42,74,.14),rgba(255,255,255,.02));
  border-color:rgba(255,42,74,.38)}
.os-item.wide .os-label{color:#ff8a9a}
.os-item.wide .os-val{color:#ff6080;text-shadow:0 0 20px rgba(255,42,74,.7)}
.over-sec-title{font-size:10px;letter-spacing:.3em;text-indent:.3em;
  color:var(--gold);opacity:.8;margin:22px 0 12px;
  display:flex;align-items:center;gap:10px;font-weight:500}
.over-sec-title::after{content:"";flex:1;height:1px;
  background:linear-gradient(90deg,rgba(212,175,55,.3),transparent)}
.over-units{display:flex;flex-wrap:wrap;gap:8px;justify-content:center}
.ou-chip{display:flex;align-items:center;gap:7px;
  padding:6px 12px;border-radius:20px;
  background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.1);
  font-size:11px;letter-spacing:.05em;color:#cfc7b6;
  transition:.3s;font-weight:400}
.ou-chip:hover{background:rgba(255,255,255,.07)}
.ou-chip.dead{opacity:.3;text-decoration:line-through}
.ou-chip.spy{border-color:rgba(255,42,74,.55);background:rgba(255,42,74,.1)}
.ou-chip .ou-dot{width:9px;height:9px;border-radius:50%;flex-shrink:0}
.ou-chip .ou-lv{font-size:9.5px;color:#8d8578;font-family:var(--font-mono)}
.over-btns{display:flex;gap:10px;margin-top:28px;justify-content:center}
.over-btns .btn{width:auto;padding:13px 42px}

.manual{position:fixed;inset:0;z-index:100;
  display:flex;align-items:center;justify-content:center;padding:20px;
  background:rgba(4,4,8,.88);backdrop-filter:blur(9px);
  opacity:0;visibility:hidden;transition:.35s}
.manual.open{opacity:1;visibility:visible}
.manual-box{width:100%;max-width:820px;max-height:88vh;overflow-y:auto;
  border:1px solid rgba(212,175,55,.4);border-radius:10px;
  background:linear-gradient(180deg,#13131c,#0a0a10);
  box-shadow:0 0 80px -20px rgba(212,175,55,.8);
  padding:30px 30px 26px;position:relative;
  transform:translateY(20px) scale(.97);transition:transform .34s var(--ease-smooth)}
.manual.open .manual-box{transform:none}
.manual-box::-webkit-scrollbar{width:6px}
.manual-box::-webkit-scrollbar-thumb{background:rgba(212,175,55,.4);border-radius:3px}
.manual-close{position:absolute;top:14px;right:16px;
  width:30px;height:30px;border-radius:50%;
  display:flex;align-items:center;justify-content:center;
  background:transparent;border:1px solid rgba(255,255,255,.14);
  color:#9d9585;font-size:17px;cursor:pointer;font-family:inherit;transition:.25s}
.manual-close:hover{color:var(--gold);border-color:var(--gold);transform:rotate(90deg)}
.manual h3{font-family:var(--font-serif);font-weight:700;
  font-size:20px;letter-spacing:.3em;text-indent:.3em;
  color:#f0e2b0;text-align:center;margin-bottom:6px;
  text-shadow:0 0 20px rgba(212,175,55,.5)}
.manual .m-sub{text-align:center;font-size:10.5px;letter-spacing:.3em;text-indent:.3em;
  color:#6f6a5e;margin-bottom:22px;padding-bottom:18px;
  border-bottom:1px solid rgba(212,175,55,.15);font-weight:300}
.m-sec{margin-bottom:22px;padding-bottom:18px;
  border-bottom:1px dashed rgba(255,255,255,.08)}
.m-sec:last-child{border-bottom:none;padding-bottom:0}
.m-sec-h{font-size:12px;letter-spacing:.28em;text-indent:.28em;
  color:var(--gold);margin-bottom:12px;display:flex;align-items:center;gap:9px;font-weight:500}
.m-sec-h::before{content:"";width:4px;height:14px;background:var(--gold);border-radius:2px;
  box-shadow:0 0 10px var(--gold)}
.m-sec p{font-size:12.5px;line-height:2;color:#a89a7a;letter-spacing:.04em;font-weight:300}
.m-sec p b{color:#f0d879;font-weight:500}
.m-row{display:flex;gap:12px;margin-bottom:11px;align-items:flex-start;
  font-size:12px;line-height:1.85;color:#9a9284;letter-spacing:.03em;font-weight:300}
.m-row:last-child{margin-bottom:0}
.m-ico{width:26px;height:26px;border-radius:50%;flex-shrink:0;
  display:flex;align-items:center;justify-content:center;
  font-size:13px;color:#fff;font-weight:500;
  background:radial-gradient(circle at 50% 30%,var(--c),color-mix(in srgb,var(--c) 60%,#000));
  border:1px solid color-mix(in srgb,var(--c) 70%,#fff);
  box-shadow:0 0 10px -2px var(--c)}
.m-row b{color:#e8d9a0;font-weight:500}
.ct-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:9px;margin-top:10px}
.ct-card{padding:10px 8px;border-radius:6px;text-align:center;
  background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07)}
.ct-card .ctn{font-size:13px;color:#fff;letter-spacing:.06em;margin-bottom:5px;font-weight:500}
.ct-card .cti{font-size:18px;margin-bottom:5px}
.ct-card .ctl{font-size:10px;color:#7ee08a;letter-spacing:.08em;line-height:1.5;font-weight:400}
.ct-card .ctl em{color:#8d8578;font-style:normal;display:block;margin-top:2px}
.dis-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin-top:8px}
.dis-card{padding:11px 12px;border-radius:6px;
  background:linear-gradient(135deg,color-mix(in srgb,var(--c) 14%,transparent),rgba(255,255,255,.02));
  border:1px solid color-mix(in srgb,var(--c) 40%,transparent);position:relative;transition:.3s}
.dis-card:hover{transform:translateY(-2px);box-shadow:0 8px 20px -10px var(--c)}
.dis-card .dn{font-size:13.5px;color:#fff;letter-spacing:.08em;
  display:flex;align-items:center;gap:7px;margin-bottom:6px;font-weight:500}
.dis-card .dn .di{font-size:16px}
.dis-card .dd{font-size:11px;color:#a89a7a;line-height:1.7;letter-spacing:.03em;font-weight:300}

.legend{display:flex;flex-wrap:wrap;gap:8px;font-size:10px;color:#7d7768;letter-spacing:.06em}
.legend .lg{display:flex;align-items:center;gap:5px;font-weight:400}
.legend .dot{width:10px;height:10px;border-radius:50%;
  border:1px solid rgba(255,255,255,.2);display:inline-block}
.legend .dot.mv{background:rgba(90,190,255,.4);border-color:rgba(120,210,255,.6)}
.legend .dot.at{background:rgba(255,80,60,.4);border-color:rgba(255,110,90,.6)}
.legend .dot.sk{background:rgba(155,89,182,.5);border-color:rgba(200,140,240,.7)}
.legend .dot.bs{background:rgba(255,42,74,.55);border-color:rgba(255,90,110,.8)}
.legend .dot.spy{background:rgba(231,76,60,.6);border-color:rgba(255,90,110,.9)}
.legend .dot.zone{background:rgba(180,20,40,.55);border-color:rgba(255,80,80,.9)}

::-webkit-scrollbar{width:8px;height:8px}
::-webkit-scrollbar-track{background:rgba(255,255,255,.02)}
::-webkit-scrollbar-thumb{background:linear-gradient(180deg,rgba(212,175,55,.5),rgba(212,175,55,.15));border-radius:4px}
::-webkit-scrollbar-thumb:hover{background:rgba(212,175,55,.7)}

/* 场景揭示弹窗 */
.scenario-reveal{
  position:fixed;inset:0;z-index:85;pointer-events:none;
  display:flex;align-items:center;justify-content:center;
  opacity:0;transition:opacity .5s}
.scenario-reveal.on{opacity:1}
.scenario-reveal .sr-box{
  padding:26px 44px;border-radius:12px;text-align:center;
  background:linear-gradient(180deg,rgba(20,20,30,.97),rgba(8,8,12,.97));
  border:2px solid rgba(212,175,55,.55);
  box-shadow:0 0 80px -16px rgba(212,175,55,.85);
  transform:translateY(20px) scale(.92);
  transition:transform .6s var(--ease-smooth)}
.scenario-reveal.on .sr-box{transform:none}
.scenario-reveal .sr-label{font-size:10px;letter-spacing:.4em;text-indent:.4em;
  color:#7d7768;margin-bottom:12px;font-weight:400}
.scenario-reveal .sr-name{
  font-family:var(--font-serif);font-weight:900;
  font-size: